package com.pharma.clientapp.repository;

import com.pharma.clientapp.entity.ClameT;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ClameTRepository extends JpaRepository<ClameT, String> {
}
