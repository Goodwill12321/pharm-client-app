package com.pharma.clientapp.repository;

import com.pharma.clientapp.entity.Ul;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UlRepository extends JpaRepository<Ul, String> {
}
