package com.pharma.clientapp.repository;

import com.pharma.clientapp.entity.ClameType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ClameTypeRepository extends JpaRepository<ClameType, String> {
}
